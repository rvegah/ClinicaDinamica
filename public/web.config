<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <!-- Habilitar reescritura de URL -->
    <rewrite>
      <rules>
        <rule name="React Routes" stopProcessing="true">
          <match url=".*" />
          <conditions logicalGrouping="MatchAll">
            <!-- NO reescribir si es un archivo físico -->
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <!-- NO reescribir si es un directorio físico -->
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
            <!-- NO reescribir si es la API -->
            <add input="{REQUEST_URI}" pattern="^/api" negate="true" />
          </conditions>
          <!-- Redirigir todo a index.html -->
          <action type="Rewrite" url="/clinica-farma/index.html" />
        </rule>
      </rules>
    </rewrite>

    <!-- Tipos MIME para SPA -->
    <staticContent>
      <mimeMap fileExtension=".json" mimeType="application/json" />
      <mimeMap fileExtension=".woff" mimeType="application/font-woff" />
      <mimeMap fileExtension=".woff2" mimeType="application/font-woff2" />
    </staticContent>

    <!-- Headers de seguridad y cache -->
    <httpProtocol>
      <customHeaders>
        <add name="X-Content-Type-Options" value="nosniff" />
        <add name="X-Frame-Options" value="SAMEORIGIN" />
        <add name="X-XSS-Protection" value="1; mode=block" />
      </customHeaders>
    </httpProtocol>

    <!-- Manejo de errores -->
    <httpErrors errorMode="Custom" existingResponse="Replace">
      <remove statusCode="404" />
      <error statusCode="404" path="/clinica-farma/index.html" responseMode="ExecuteURL" />
    </httpErrors>
  </system.webServer>
</configuration>