import { Typography, Box } from '@mui/material';

const UserManagementPage = () => {
  return (
    <Box>
      <Typography variant="h4">Gestión de Usuarios - En construcción</Typography>
    </Box>
  );
};

export default UserManagementPage;